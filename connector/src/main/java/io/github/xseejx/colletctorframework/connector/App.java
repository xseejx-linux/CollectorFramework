package io.github.xseejx.colletctorframework.connector;


public class App 
{
    //TODO: Implement connector to expose collector execution via API or CLI
    // Let me tell you what this does:
    /*
    When you start the program, congratulations you have started this cancer!
    
    This will be able to get inboud calls from the internet, and START A COLLECTOR, via CollectorRequestActivator

    Then once CollectorRequestActivator does its stuff gets the json results and send it back. (TO THE SERVER)

    OR

    From an external tools, call the programm with "special parameter": collector, parameter

    (e.g fim.filehash, /path/file)

    And this will execute collector get data and send it to ... SERVER (which is the same as before)

    OR if special apps wants to be special, it can gently ask to do not send data back to server, but gently send data back to ITSELF.

    BUT YOU CAN FORCE IT BY SENDING ALL ALWAYS BACK TO THAT SERVER !!

    Still to implement, so do not dream too much.
     */
}
